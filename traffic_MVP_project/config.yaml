camera_id: "cam-01"

count_line:
  p1: [100, 1200]
  p2: [1000, 1200]

vehicle_classes: ["car", "truck", "bus", "motorcycle"]

speed_ref_kmh: 30.0
density_ref_count: 20

# Fallback (si homography disabled)
pixels_per_meter: 8.0

homography:
  enabled: true
  # 4 points pixels (HG, HD, BD, BG) - A REMPLIR
  src_px: [[300, 600], [800, 600], [1050, 1500], [120, 1500]]

  # 4 points en mètres correspondants (exemple rectangle 6m x 12m)
  # Ajuste selon ta route (largeur voie ~3.5m, 2 voies ~7m, etc.)
  dst_m: [[0, 0], [6, 0], [6, 12], [0, 12]]
